import React, { Component } from 'react';

class Profile extends Component {
	constructor(props) {
		super(props);
	}

	render() {
		return <div>This is the profile for now...</div>;
	}
}

profile.propTypes = {};

export default Profile;
